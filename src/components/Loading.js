'use strict';

import React from 'react';
import {
  ActivityIndicator,
  Text,
  View,
  Image,
  ImageBackground,
} from 'react-native';

export default function() {
  return (<ActivityIndicator
        hidden='false'
        size='large'/>);
}
